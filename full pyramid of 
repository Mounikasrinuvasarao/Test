#include<stdio.h>
int main()
{
 int i, gap, row, k=0;
 printf("enter the number of rows: ");
 scanf("%d", &rows);
 for(i=1;i<=rows;i++,k=0)
 {
  for(gap=1;gap<=rows-i;gap++)
   {
    printf(" ");
   }
   while(k!=2*i-1)
   {
    printf("*");
    k++;
   }
   printf("\n");
 }
 return 0;
}
